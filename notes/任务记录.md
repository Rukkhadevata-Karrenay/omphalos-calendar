# 任务记录

## 2026-06-14 视觉修正与时间模块

- 首页封面改用用户提供的完整昔涟原图语义资产 `public/assets/omphalos/overview/home-omphalos-year-original.jpg`；原图自带“翁法罗斯一年历”标题，因此 React 叠加层隐藏中文大标题，只保留英文说明、说明文字和真实可见按钮。
- 时间模块背景保持完整横图比例，不裁剪；表盘位置保持在图中月亮/天体处，信息窗放在底部并随容器缩放。
- 指针使用用户提供的透明 PNG 色调版；旋转中心按 PNG 下方圆孔实际位置校准，指针长度控制在表盘外圈内。
- 有待办的日期格不显示数字气泡；黄紫配色待办在日期格上使用完整黄紫分割边框，内部仍保留日期裁片。
- 月首神谕语句已确认由对应黄金裔署名，语句内 `「」` 成对；只隐藏首页重复标题，不删除原图标题。
- 验证：`npm run test:time` 通过，`npm run build` 通过；浏览器控制台无 error。

## 2026-06-14 月份裁片与专属日期修正

- 11 月 12 日设置为只读系统日程「翁法罗斯之日」，通过 `src/data/omphalosSpecialDays.ts` 叠加展示，不写入 localStorage，不改变 `CalendarEvent` 结构。
- 年总览月份卡角标从 `01`、`02` 改为 `1月`、`2月` 等中文月份格式。
- 日期格黄紫待办边框改为包裹整个日期块；「翁法罗斯之日」日期格使用红、黄、蓝、紫、绿、粉、青专属彩框。
- 修复欢喜月与收获月错位：问题来自 `public/assets/omphalos/date-buttons/05/` 和 `08/` 的单日裁片纵向裁偏，且按钮叠加到底板后仍会从缝隙透出原底板数字；已按原日历底板数字中心重裁这两个月全部日期裁片，并给 5 月/8 月日期按钮区增加不透明遮底层。
- 「翁法罗斯之日」专属日程备注按用户要求改为 `「你好，世界」`，日程卡保留七色渐变直接填充。
- 验证：浏览器检查 `/#month-05`、`/#month-08`，日期数字已回到各自块内且无原底板数字残留；检查 `/#month-11`，专属日程备注和渐变背景生效；`npm run build` 通过。

## 2026-06-15 渐变与月份日期层修正

- 移除 5 月/8 月日期区额外 grid 遮底层，恢复为和其他月份一致的“底板 + 单日按钮裁片”叠法，避免看起来多贴了一层日期模块。
- 将「翁法罗斯之日」日期框和专属日程背景从硬分段彩虹改为连续多色渐变，避免明显色块分割。
- 保持 `CalendarEvent` 字段和 `omphalos-calendar-events-v1` localStorage key 不变。
- 验证：`npm run test:time` 通过，`npm run build` 通过；浏览器检查 `/#month-05`、`/#month-08` 的 grid 伪层为 `none`，`/#month-11` 的日期框和系统日程均为连续 `linear-gradient`。

## 2026-06-15 日期按钮统一重生成

- 按用户提供的新版框线参考，新增 `scripts/generate-date-button-assets.py` 的 clean 面板生成流程：原始 `calendar-panels` 保留备查，页面改用 `calendar-panels-clean`。
- 重新生成 12 个月的 `date-buttons/<month>/<day>.jpg` 与 `blank.jpg`：所有月份复用统一 7x6 坐标，空日期也有独立按钮块覆盖，避免未来年份改变星期后露出固定年份底图数字。
- 哀悼月 12 日单独从原图无穷符号真实位置取样，保留 `∞` 原图标志；「翁法罗斯之日」日程背景和日期框继续使用连续多色渐变。
- 移除移动端中破坏坐标比例的 `.image-calendar-panel .calendar-grid { left/right: 7%; }` 覆盖，窄屏和桌面使用同一套原图坐标缩放。
- 验证：已生成本地组合预览 `/tmp/omphalos_preview_01.jpg`、`/tmp/omphalos_preview_05.jpg`、`/tmp/omphalos_preview_08.jpg`、`/tmp/omphalos_preview_11.jpg`，未见旧数字残留；`npm run build` 通过，浏览器 DOM 检查确认页面使用 `calendar-panels-clean` 与新版 `date-buttons`。

## 2026-06-18 6 月完整模板 overlay 校准

- 进入视觉稳定阶段：月份图片按最终 UI 模板处理，完整显示且禁止裁切、拉伸和 `cover`。
- 新增 `src/components/TemplateMonthView.tsx`，只负责相对定位模板和绝对定位日期 overlay；日期选择、日程与 localStorage 逻辑保持不变。
- 6 月坐标集中在 `src/data/monthLayoutConfig.ts` 的 `JUNE_LAYOUT`，按模板实际日期区从 6 行修正为 5 行；30 号和尾部空白格均位于第 5 行。
- 6 月暂时启用 `.debug-layout`，显示红色 overlay 外框和日期格半透明背景，等待用户视觉确认后再关闭。
- 新增 `docs/视觉对齐规则.md` 和 `docs/assets-check-report.md`，记录模板、坐标、素材与验证约束。
- 验证：`npm run build` 通过；`http://localhost:5173/#month-06` 显示 35 个等尺寸日期格（30 个按钮、5 个非交互空白格），15 号、30 号和空白格尺寸一致，页面控制台无 error。

## 2026-06-18 6 月素材合成改造

- 用户明确纠正素材职责：`month-template-bases` 是正式底图，`calendar-panels-clean` 覆盖原日期区，`date-buttons` 提供真实按钮图，`final-month-templates` 只作对照。
- 建立 `public/assets/calendar-composition/` 四类目录并复制原始素材；未修改图片内容。
- 新增 `monthCompositionAssets.ts`、`monthButtonLayoutConfig.ts` 和 `MonthCompositionView.tsx`；新合成结构只对 6 月启用，其他月份保持旧渲染。
- 6 月固定渲染 6×7 共42格，30张日期图和12张 `blank.jpg`；视觉图片层与透明热区层逐格重合。
- clean panel 通过底图边缘像素匹配确定原始起点 `y=1700`，最终 `panelTop=69.7865%`、`panelHeight=26.6831%`，解决估算 73.3% 时星期行重复露出的问题。
- 新增 `npm run check:assets`，禁止正式源码引用 `final-reference` 或 `final-month-templates`。
- 验证：资产检查与构建通过；浏览器 DOM 无参考图路径，点击 15 号可打开日期详情，控制台无 error。

## 2026-06-18 6 月正式显示与五周布局

- 6 月正式页默认关闭 `.debug-layout`，保留 `JUNE_COMPOSITION_DEBUG` 单一开关和原调试样式。
- `src/data/monthButtonLayoutConfig.ts` 同时保留 `fiveWeek` 与 `sixWeek`；`defaultRows=5` 控制当前 35 格，未来改为 `6` 即切换到 42 格。
- 默认五周坐标为 `gridTop=15.23%`、`gridLeft=11.69%`、`gridWidth=77.91%`、`gridHeight=55.54%`；六周配置继续保留原 42 格坐标与素材。
- 6 月“本月日程”从右侧黑色玻璃卡改为日历下方同宽的黄紫渐变圆角填充框，不改日程数据逻辑。
- 新增首页专用可选字段 `homeHeroLine`；6 月显示 `「晨昏之眼」艾格勒 /「天空的医师」雅辛忒丝`，月份详情继续使用 `deity + motto` 原文。
- 验证：`npm run check:assets`、`npm run build` 通过；浏览器实测 35 图像格/35 热区、debug 关闭、最终参考图未进入 DOM、控制台无 error。

## 2026-06-18 6 月底图版权区恢复

- 暂停日期和日程调整，按“文件来源 → 图片内容 → CSS 容器 → overlay 边界”顺序排查底图完整性。
- 确认正式底图与 `month-template-bases/06-long-day-month-base.jpg` 哈希一致，尺寸 1125×2436，文件自身包含底部版权信息。
- 根因是通用 `.image-calendar-panel` 仍固定 `aspect-ratio: 1125 / 2350` 并隐藏溢出，裁掉原图最后 86px。
- 仅在 `src/App.css` 为 `.month-composition-view` 恢复 `1125 / 2436` 并允许完整显示；未改素材、日期坐标或日程逻辑。
- 浏览器确认版权可见、clean panel 在版权区前结束、日程模块位于底图容器外、正式 DOM 无 final reference。

## 2026-06-18 6 月真正五周布局与日程日期框

- `monthButtonLayoutConfig.ts` 改为 `defaultLayout`，`fiveWeek` 和 `sixWeek` 各自保存完整 panel/grid 配置与 `totalCells`；当前默认 `fiveWeek`，六周素材和 42 格逻辑保留。
- 按 5 周参考图测得五行中心约为 panel 内 `165/245/325/405/485px`，对应 `gridTop=20.31%`、`gridHeight=59.38%`、`gapY=3.63%`，不再使用六周坐标的前五行。
- 6 月隐藏底部独立“本月日程”卡；日程状态在对应 `.date-button-cell` 内以绝对定位 overlay 呈现。
- 用户进一步纠正：日程状态不是内部圆角胶囊，而是 `.date-event-frame` 黄紫渐变框包裹整个日期按钮，保持日期数字可见。
- 18 号 today outline 位于更高层；16 号日程框和 today 状态互不改变按钮尺寸与网格布局。
- 浏览器实测 35 张日期图、35 个热区、1 个 16 号日程框、0 个独立日程卡、0 个 debug 类，正式 DOM 无 final reference。

## 2026-06-18 6 月面板接缝与日程整格填充精修

- 只调整 6 月视觉：在 `monthButtonLayoutConfig.ts` 增加 `panelMaskImage`，由组件传给 clean panel；双轴渐隐重点处理右侧装饰边框和右下角硬接缝。
- 根据用户最新纠正，将 `.date-event-frame` 从空心渐变框改为 `inset: 0` 的整格半透明黄紫渐变填充，16 号日期数字通过透明度保持可读。
- 图层保持：按钮图片底层、日程填充 `z-index: 2`、today outline 与透明热区 `z-index: 5`；互不改变按钮尺寸。
- 浏览器复核仍为真正 fiveWeek 7×5 共 35 格，版权可见，正式 DOM 未引用 final reference。

## 2026-06-19 6 月 fiveWeek 专用 clean panel

- 用户确认不再用 CSS mask 掩盖旧 JPG 的硬边，改为从正式底图 `y=1700..2350` 生成 fiveWeek 专用透明 PNG。
- 新增 `scripts/generate-fiveweek-clean-panel.mjs`，使用 ImageMagick 中值柔化、Gaussian blur 和圆角 alpha feather；不使用 AI，不读取 final reference，不修改正式底图。
- 6 月 fiveWeek 映射到 `06-aigle-long-day-month-fiveweek-clean-panel.png` 并关闭 CSS mask；sixWeek 保留旧 JPG、旧 mask 和 42 格配置。
- `npm run check:fiveweek-panel` 自动检查 1125×650、RGBA、透明外边缘、连续 alpha、第六周透明、布局映射以及底图/旧 panel hash。
- 浏览器复核：新 PNG、`mask-image: none`、35 张按钮、35 个热区、版权可见，正式 DOM 无 final reference。

## 2026-06-19 fiveWeek panel 内部均匀度修复

- 用户截图显示首排上方仍有色带、末排及 30 号后空白区域仍有色差；未修改按钮、网格或 35 格逻辑。
- 分层和源图检查发现旧 PNG 仍保留底部数字低频影子，且 Gaussian alpha 在五周核心区未完全不透明，使原底图继续透出。
- 生成脚本改为 41×41 中值滤波、6% 低频采样、恢复 1125×650 后 `0x20` blur；alpha 改为顶部 `105..140`、底部 `520..548` 的明确分段过渡，核心区保持 alpha=1。
- 浏览器完成 base-only、base+panel、完整按钮三层复核并恢复正常显示；实测35张按钮、35个热区、1个日程层、19号 today 高亮、版权可见、DOM 无 final reference。

## 2026-06-19 程序化 clean panel 与日期格圆角统一

- 用户否决继续使用整块模糊、低频采样和 alpha=1 核心：该方式会形成灰粉色矩形贴片，不能仅以自动检查通过作为视觉完成。
- `generate-fiveweek-clean-panel.mjs` 改为纯程序化二维综合色板；最终参考图只用于人工量色，脚本运行时不读取参考图、旧 panel 或正式底图像素，不使用 AI。
- 新 PNG 保持 1125×650 RGBA、四边 alpha 羽化和第六周透明；专项检查禁止旧 Median/6%/大 blur 管线，并检查首末日期行附近的平均亮度连续性。
- `src/App.css` 新增 `--date-cell-radius`，普通、空白、16 号日程与 19 号 today 状态统一圆角；按钮仍使用 `date-buttons/06/` 图片和 `object-fit: contain`。
- 浏览器完成 base-only、base+panel、完整页面三层检查并恢复全部图层：35 张按钮、35 个热区、版权可见、正式 DOM 无最终参考图。

## 2026-06-19 6 月日期按钮素材归一化

- 用户进一步确认局部色块来自原始逐日 JPG 自带的底色差异，不再调整 clean panel 或日期坐标。
- 新增 `generate-normalized-date-buttons.mjs`，使用 `blank.jpg` 单一母版生成 32 张统一 PNG；数字写入图片，不使用 CSS 或 AI 重画按钮。
- `monthCompositionAssets.ts` 按布局区分按钮目录：fiveWeek 使用 normalized PNG，sixWeek 保留原始 JPG；原目录 aggregate hash 未变化。
- 新增 `check:date-buttons`，验证数量、118×66 PNG、边缘模板一致、色差降低、目录映射与正式组件禁用参考图。
- 浏览器确认 35 张按钮和 35 个热区，16 号日程、19 号 today、clean panel 与版权均保持正常。

## 2026-06-20 6 月 uniform 按钮候选生成

- 用户否决上一版 normalized 的页面视觉，要求先生成候选并人工查看预览，未确认前禁止切换页面。
- 新增 `generate-uniform-date-buttons.mjs` 和 `check-uniform-date-buttons.mjs`；以原始 `blank.jpg` 单一母版生成 blank、01..31 共 32 张 JPG。
- 所有候选为 118×66 sRGB JPEG，背景 RGB 检查为零离散，外部边框区域一致；原始与 normalized 目录 hash 未变化。
- `docs/button-uniform-preview-06.png` 已生成；当前 fiveWeek 仍使用 normalized 目录，等待用户确认。

## 2026-06-20 clean PNG 母版重建

- 用户否决原 blank.jpg 母版和 uniform JPG 批次，原因是原图/JPEG 边缘仍带黑边与脏点；禁止继续批量生成。
- 新增 `generate-clean-button-master.mjs` 与 `check-clean-button-master.mjs`，程序化重建 `master-clean.png`，不复制 JPG 像素。
- 生成原图对比和 400%/800% 放大预览；检查确认 118×66 RGBA、无近黑边、内部平滑。
- 当前未批量生成 uniform PNG，未修改页面、fiveWeek、clean panel 或其他月份。
- 用户指出原 blank 顶部是一整块深色分区，不是可保留的高光；clean 母版已改为全区域统一粉紫底色，仅保留一条浅色高光线，检查顶部与主体 RGB 距离为 0。
- 用户再次指出独立浅色高光线仍是横向色带；已移除该线，改为整块底色连续轻渐变，并新增相邻扫描行 RGB 跳变阈值检查。

## 2026-06-20 uniform PNG 批量生成（未接入）

- 用户人工确认 `master-clean.png` 后，允许批量生成但明确禁止切换页面引用。
- `generate-uniform-date-buttons.mjs` 从同一个 118×66 RGBA 母版生成 `blank.png`、`01.png` 至 `31.png` 共 32 张图片；blank 直接复制母版，其余只叠加统一字体、字号、颜色和阴影的数字。
- `check-uniform-date-buttons.mjs` 验证输出数量、尺寸、色彩模式、母版继承、可见边缘一致性、顶部扫描行连续性、原始与 normalized 目录完整性及页面映射未切换。
- 预览图为 `docs/button-uniform-preview-06.png`；当前 6 月 fiveWeek 仍读取 `date-buttons-normalized/06/`，等待用户确认后再决定是否接入。

## 2026-06-20 6 月 fiveWeek 接入 uniform PNG

- 用户确认 uniform 预览后，仅将 6 月 `dateButtonDirs.fiveWeek` 切换到 `/assets/calendar-composition/date-buttons-uniform/06/`。
- `sixWeek` 继续使用 `/assets/calendar-composition/date-buttons/06/` 和 JPG；其他月份、clean panel、布局坐标及组件结构未修改。
- `check-uniform-date-buttons.mjs` 已更新为检查该布局隔离关系；专项检查和 `npm run build` 均通过。

## 2026-06-20 12 个月统一素材合成

- 用户确认 6 月后，以其为唯一模板将 1–12 月全部切换到 `MonthCompositionView`；没有修改图片素材、路由、localStorage、首页布局或 6 月验收坐标。
- 12 个月均在 `monthCompositionAssets.ts` 中配置 two-layout panel/button 映射，在 `monthButtonLayoutConfig.ts` 中配置 fiveWeek 35 格和 sixWeek 42 格。
- 2026 年默认布局：5 月、8 月为 sixWeek；1–4、6–7、9–12 月为 fiveWeek。6 月继续使用 fiveWeek clean PNG 与 uniform PNG。
- 11 月 12 日“翁法罗斯之日”继续保存在 `omphalosSpecialDays.ts`；合成组件新增 `.date-special-frame` 独立描边层，保留 12.jpg 的“∞”图案，并可与日程和 today 层共存。
- 浏览器逐月检查 base/panel 加载、按钮/热区数量、debug、旧 cell、final reference 和特殊日；无控制台警告或错误。
- 除 6 月外的 11 个月先完成结构接入，仍需用户按最终参考图逐月确认像素级坐标和 panel 融合。

## 2026-06-20 全月份按钮归一化与特殊日彩框

- 用户验收时指出：除 6 月外仍有逐格按钮色差，11 月“翁法罗斯之日”边框不是彩色。
- 量化确认 11 个月页面仍读取原始逐日 JPG；新增脚本从各月单一主题色生成 clean RGBA 母版与 32 张 uniform PNG，6 月目录保持不变。
- 1–5、7–12 月 fiveWeek/sixWeek 均切换到各自 uniform PNG；6 月 fiveWeek/sixWeek 映射保持已验收状态。
- 11 月 12 日通过集中映射继续读取原始 `11/12.jpg` 的“∞”图案，独立 overlay 使用 8 色 `border-image`，不覆盖图案且不改变格子尺寸。
- 浏览器检查 12 个月按钮/热区数量和图片路径；新增色差检查、素材检查与 build 均通过。

## 2026-06-20 发布前冻结检查

- 冻结期间未修改页面逻辑、路由、localStorage 结构或任何图片素材；仅补齐 `.gitignore`、README 发布信息和 `NOTICE.md`。
- `npm run check:assets` 与 `npm run build` 均通过，`dist/index.html` 已生成；生产构建约 333 MB、1448 个文件。
- `npm run preview` 在 `http://localhost:4173/` 可用；逐月检查 1–12 月 base、panel、日期按钮、35/42 格热区均正常，0 张图片加载失败，正式 DOM 无 final reference。
- 6 月仍使用已验收 fiveWeek clean panel 与 uniform PNG；当前日期 20 日高亮存在。11 月 12 日继续使用“∞”按钮，并由独立 8 色 `border-image` overlay 显示。
- 生产预览刷新后 hash 页面未白屏；`omphalos-calendar-events-v1` 在刷新前后保持一致，控制台无 warning/error。
- 当前目录是上层 `~/Codex_Workspace` Git 仓库中的未跟踪子目录，不是独立仓库；发布前应在项目目录执行 `git init`。node_modules、dist、env 和 `.DS_Store` 均被忽略，没有单文件超过 GitHub 50 MiB 警告阈值。
- 上线前风险：构建产物约 333 MB，Vercel 官方限制页面在当前网络环境无法核实；首次部署需人工观察平台是否接受该体积，不应通过修改或压缩已冻结素材规避。

## 2026-06-21 首页时间同步与移动端封面安全区

- 将首页唯一的当前时间来源集中到 `useCurrentTime`：首次挂载、每秒 tick、页面回到前台和窗口重新聚焦时都直接读取 `new Date()`，避免后台计时器节流后继续显示旧时间。
- 移动端（≤768px）完整显示昔涟封面图，标题说明与按钮改放在图片下方的内容自适应安全面板；桌面端继续使用原叠加布局，月份详情页未改。
- 390/414/430/768px 均无文字叠压或横向溢出；安全面板没有大段空白，因此未追加开拓者图片。艾格勒神谕背景按用户决定复用 `hyacinthia.jpg`。
- `npm run test:time`、神谕映射专项检查、`npm run check:assets` 与 `npm run build` 全部通过；浏览器确认 6 月仍为 35 个按钮和 35 个热区，当日 21 日高亮正常。

## 2026-06-21 首页安全断点纠正与一年历 oracle 头图

- 用户明确纠正：图下安全面板不是覆盖全部移动端，而是只在图内文案确实会与原图文字冲突时启用。断点由 `768px` 收窄为 `480px`；481px、569px、768px 和桌面继续使用封面内叠加布局。
- 浏览器验证 390/430/480px 使用图下安全面板，481/569/768/1280px 文案和按钮均在封面内部，所有宽度无横向溢出。569px 下文案位于原图大标题下、版权区上，未发生叠压。
- 一年历总览头图背景层和内部 `<img>` 统一读取当天神谕 speaker 对应的 oracle 图片；2026-06-21 使用 `cyrene.jpg`，旧 `home-omphalos-year-clean.jpg` 不再残留于总览头图。
- 新增响应式断点与一年历图片接入回归检查；`npm run test:time`、`npm run check:assets`、oracle 专项检查和 `npm run build` 全部通过。

## 2026-06-21 最终背景分层与缩放安全模块

- 页面最底层和一年历总览头图背景均绑定当天神谕 speaker 对应的 oracle 图；总览头图内部图片恢复为 `/assets/omphalos/eggs/xilian-demiurge-card.jpg`，不再与背景共用 oracle 图。
- 安全模块按 CSS 有效视口处理浏览器缩放：≤1080px 独立到完整封面图下方，1081px 起恢复图内叠加。390/569/768/1024/1080/1081/1280px 均无横向溢出。
- 481–1080px 安全模块使用 `home-safe-cyrene.png`；≤480px 使用 `home-safe-calendar-title.png`。两张图从用户指定原图逐字节复制到正式 overview 目录，原始素材未修改。
- 浏览器确认 2026-06-21 的全局底图和总览背景均为 `oracle-backgrounds/cyrene.jpg`，内部图为指定 JPG，0 张图片加载失败。
- 响应式、oracle、时间、素材引用检查及 build 全部通过。
